<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send everything through public/ so shared hosting can serve the
    # Laravel app from the repo root without changing document_root.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
